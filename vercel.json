{
  "version": 2,
  "buildCommand": "cd hms-backend && npm run build && cp -r dist api/ && cd ../hms-frontend && npm run build",
  "outputDirectory": "hms-frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
