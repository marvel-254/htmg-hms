{
  "version": 2,
  "buildCommand": "cd hms-backend && npm ci --ignore-scripts && npm run build && cd ../hms-frontend && npm ci --ignore-scripts && npm run build",
  "outputDirectory": "hms-frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ],
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
